# v0.3 Static Batching

## Goal

Run multiple requests together in a fixed batch.

## Why

Static batching makes the batch dimension explicit before introducing continuous batching.

## Scope

- fixed batch queue
- padding
- attention masks
- batch decode loop
- whole-batch completion

## Out Of Scope

- mid-batch request admission
- per-request exit without rebuilding the batch

## Acceptance Criteria

- Multiple prompts can be processed in one model forward path.
- Batch size is visible in benchmark output.
- The limitations of static batching are documented.

Current limitation statement:

- The batch is fixed for the full decode trajectory; there is no mid-batch admission,
  preemption, request finishing-and-repacking, or shared work stealing in this milestone.

## Progress

- [x] Add fixed batch input handling.
- [x] Add padding and attention masks.
- [x] Add batch prefill.
- [x] Add batch decode.
- [x] Add benchmark for batch size changes.
- [x] Document why static batching is not enough.
- [x] Add static batch tests.
- [x] Export generate_batch in engine package API.

## v0.10 Carry-forward (Responses API parity subset)

The responses API milestones added in v0.10 are partially folded into v0.3 here for teaching-ready correctness:

- [x] Add `POST /v1/responses/{response_id}/cancel`.
- [x] Keep and return response lifecycle status for stored responses.
- [x] Support `previous_response_id` continuation for text-only requests.
- [x] Add ordered streaming event sequencing (`sequence_number`) for response SSE.
- [ ] Add full tool-call shaped response items and tool output events.
- [ ] Add true `background`/async responses and stream retrieve.
- [ ] Add structured output and detailed token usage parity.

### Cross-Reference Notes (vLLM / SGLang)

- Added endpoints in this milestone:
  - `/v1/responses/{response_id}` (existing) and `/v1/responses/{response_id}/cancel` (new).
  - SSE event sequencing with `sequence_number`.
  - Continuation via `previous_response_id`.
- Still missing compared with vLLM/SGLang:
  - no long-lived background response object lifecycle,
  - no tool-call event graph,
  - no async/background execution or full streaming retrieve API,
  - no structured output/config schema enforcement.
